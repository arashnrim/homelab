services:
  app:
    image: ghcr.io/etechonomy/joplin-server:3.4.4
    container_name: joplin-app
    depends_on:
      - db
    ports:
      - "22300:22300"
    restart: unless-stopped
    env_file:
      - stack.env

  db:
    image: postgres:16
    container_name: joplin-db
    volumes:
      - pg_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    restart: unless-stopped
    env_file:
      - stack.env

volumes:
  pg_data:
